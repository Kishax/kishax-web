import express, { Request, Response, NextFunction } from 'express';

const router = express.Router();

router.get("/", async (_: Request, __: Response, ___: NextFunction) => {
    //
});

export default router;
